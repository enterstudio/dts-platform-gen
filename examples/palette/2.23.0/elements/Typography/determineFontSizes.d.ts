import { FontFamily } from "./Typography"
/**
 * Determines which font sizes/line heights to use for typography.
 */
export declare function determineFontSizes(fontType: keyof FontFamily, size: string | string[]): any
